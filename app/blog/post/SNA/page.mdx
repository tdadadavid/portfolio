{/* ========================= SEO/METADATA =========================== */}

import { buildPostMetadata } from '@/lib/blogs';
import info from '@/misc/info';

export const key = 'SNA';

export const metadata = buildPostMetadata(key, { me: info.me, icon: '/icon.svg' });

{/* ================================================================== */}

# Shared-Nothing Architecture

This article is still being written.
